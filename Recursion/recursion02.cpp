//La suma máxima de elementos de un vector
#include <iostream>
#include <vector>

using namespace std;

int main(){
	vector<int> vector();
	retun 0;
}

int suma(vector<int> vector, int indice){
	if(indice+1 == elementos.size())
		return vector[indice];
	return (suma(vector, indice+1) + vector[indice]);
}