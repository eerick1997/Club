#include <bits/stdc++.h>

using namespace std;

string solution( string a, string b , int i ){

    if( i >= a.size() )
        return a;
    if(  )
}

int main(){
    int q;
    string a, b;
    cin >> q;
    while( q-- ){
        cin >> a >> b;
        cout << solution( a, b );
    }
    return 0;
} 