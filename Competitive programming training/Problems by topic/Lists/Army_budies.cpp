#include <bits/stdc++.h>

using namespace std;

int main() {
    int soldiers, reports, L, R;
    while( cin >> soldiers >> reports and soldiers and reports ) {
        while( reports-- ) {
            cin >> L >> R;

        }

        cout << "-" << endl;
    }
    return 0;
}