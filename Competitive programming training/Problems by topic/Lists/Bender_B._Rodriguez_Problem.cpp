#include <bits/stdc++.h>

using namespace std;
using long long int = lli;

int main() {
    lli length;
    string buffer;
    while( cin >> length and length ) {
        for( int j = 0; j < length - 1; j++ ) {
            cin >> buffer;
        }
    }

    return 0;   
}