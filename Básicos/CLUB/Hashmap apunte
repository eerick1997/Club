//**Hashmaps**/
#include <unorderer_set>
#include <unorderer_map>

using namespace std;

int main(){
	
	//
	unorderer_set<T> conjunto;
	conjunto.insert(x); O(1)
	conjunto.erase(x); O(1)
	conjunto.count(x); O(1)
	conjunto.find(x); O(1)

	//
	unorderer_map<T,U> mapeo;
	mapeo[1] = "marcos"; 
	mapeo[-1] = "Mary"; O(1)
	return 0;

	//Trie insertamos 
	
}	

