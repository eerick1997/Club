#include <bits/stdc++.h>

using namespace std;
typedef long long int lli;

int main(){
    string r, s;
    cin >> r >> s;
    for(int i = 0; i < s.size(); i++){
        if(int a = 0){

        }
    }

    for(int i = r.size() - 1; i >= s.size(); i--){
        
    }

    return 0;
}