#include <bits/stdc++.h>

using namespace std;
typedef long long int lli;

int main(){
  ios::sync_with_stdio( 0 );
  cout.tie( 0 );
  cin.tie( 0 );
  int n, v;
  vector< int > N;
  cin >> n;
  N.resize( n, 0 );
  for( int i = 1; i <= n; i++ ){
    cin >> N[ i ];
    
  }
  return 0;
}