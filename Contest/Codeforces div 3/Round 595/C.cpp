#include <bits/stdc++.h>

using namespace std;
typedef long long int lli;

int main(){
    lli q;

    cin >> q;
    while( q-- ){
        lli n;
        cin >> n;
        vector< lli > V( n );
        for( int i = 0; i < n; i++ )
            cin >> V[ i ];
        
    }
    return 0;
}